/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.unsplash.com",
      "plus.unsplash.com",
      "ipfs.io",
      "*.nectarnode.io",
      "*.ipfs.nectarnode.io",
      "ipfs.nectarnode.io",
    ],
  },
};

export default nextConfig;
